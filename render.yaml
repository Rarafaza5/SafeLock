services:
  - type: web
    name: safelock-relay
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RELAY_SECRET
        generateValue: false
        value: admin123
      - key: NODE_ENV
        value: production
    healthCheckPath: /
